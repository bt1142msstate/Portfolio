{
  "name": "personal-site",
  "private": true,
  "scripts": {
    "build:site": "tsc assets/ts/site.ts --outDir assets/js",
    "build:resume": "tsc resume/print.ts --outDir resume",
    "build": "npm run build:site && npm run build:resume"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
